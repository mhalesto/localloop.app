// Update apiBaseUrl to point the marketing site at your production backend when hosting on GitHub Pages.
// Example: window.LOCALLOOP_SITE_CONFIG = { apiBaseUrl: 'https://api.localloop.app' };
window.LOCALLOOP_SITE_CONFIG = window.LOCALLOOP_SITE_CONFIG || {
  apiBaseUrl: ''
};
